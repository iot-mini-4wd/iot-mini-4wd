#coding:utf-8
from setuptools import setup

package_name="mini4wd"

setup(
    name=package_name,
    version='0.0.1',
    description='Library to control the mini-4WD in the IoT',
    packages=[package_name,],
)
